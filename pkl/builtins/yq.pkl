import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
    category = "Data Formats"
    description = "YAML processor"
}
yq = new Config.Step {
    glob = "**/*.yaml"
    stage = "<JOB_FILES>"
    check = "yq {{ files }}"
    fix = "yq -iP {{ files }}"
}
