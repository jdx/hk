import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
    category = "Python"
    description = "Python import sorter"
}
isort = new Config.Step {
    glob = "**/*.py"
    stage = "<JOB_FILES>"
    check = "isort --check-only {{ files }}"
    fix = "isort {{ files }}"
}
