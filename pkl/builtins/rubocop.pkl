import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Ruby"
  description = "Ruby style guide"
}
rubocop = new Config.Step {
  types = List("ruby")
  stage = "<JOB_FILES>"
  check = "rubocop {{ files }}"
  fix = "rubocop --fix {{ files }}"
}
