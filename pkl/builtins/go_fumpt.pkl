import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Go"
  description = "Strict Go formatter"
}
go_fumpt = new Config.Step {
  glob = "**/*.go"
  check = "gofumpt -l {{ files }}"
  fix = "gofumpt -w {{ files }}"
}
