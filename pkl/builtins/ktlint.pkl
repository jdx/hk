import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
    category = "Other Languages"
    description = "Kotlin linter and formatter"
}
ktlint = new Config.Step {
    glob = "**/*.kt"
    stage = "<JOB_FILES>"
    check = "ktlint {{ files }}"
    fix = "ktlint -F {{ files }}"
}
