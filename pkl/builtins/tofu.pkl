import "../Config.pkl"

tofu = new Config.Step {
    glob = List("**/*.tf", "**/*.tfvars", "**/*.tftest.hcl")
    stage = glob
    check_list_files = "tofu fmt -check {{ files }}"
    fix = "tofu fmt {{ files }}"
}
