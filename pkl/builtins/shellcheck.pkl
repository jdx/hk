import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Shell"
  description = "Shell script analyzer"
}
shellcheck = new Config.Step {
  batch = true
  glob = List("**/*.sh", "**/*.bash")
  check = "shellcheck {{ files }}"
}
