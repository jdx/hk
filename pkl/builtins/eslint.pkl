import "../Config.pkl"

eslint = new Config.Step {
    glob = List("**/*.js", "**/*.jsx", "**/*.ts", "**/*.tsx")
    stage = "<JOB_FILES>"
    batch = true
    check = "eslint {{ files }}"
    fix = "eslint --fix {{ files }}"
}
