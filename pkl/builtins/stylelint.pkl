import "../Config.pkl"

stylelint = new Config.Step {
    glob = List("**/*.css", "**/*.scss", "**/*.sass", "**/*.less")
    stage = glob
    check = "stylelint {{ files }}"
    fix = "stylelint --fix {{ files }}"
}
