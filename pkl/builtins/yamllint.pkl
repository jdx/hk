import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
    category = "Data Formats"
    description = "YAML linter"
}
yamllint = new Config.Step {
    glob = List("**/*.yml", "**/*.yaml")
    check = "yamllint {{ files }}"
}
