import "../Config.pkl"

ox_lint = new Config.Step {
    glob = List("**/*.js", "**/*.jsx", "**/*.ts", "**/*.tsx")
    stage = "<JOB_FILES>"
    check = "oxlint {{ files }}"
    fix = "oxlint --fix {{ files }}"
}
