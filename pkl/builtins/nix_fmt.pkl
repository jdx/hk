import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
    category = "Nix"
    description = "Nix formatter"
}
nix_fmt = new Config.Step {
    glob = "**/*.nix"
    stage = "<JOB_FILES>"
    check = "nixfmt --check {{ files }}"
    fix = "nixfmt {{ files }}"
}
