import "../Config.pkl"

black = new Config.Step {
    // Matches both .py files and Python scripts without extensions (via shebang)
    types = List("python")
    stage = "<JOB_FILES>"
    check = "black --check {{ files }}"
    check_diff = "black --diff {{ files }}"
    fix = "black {{ files }}"
} 
